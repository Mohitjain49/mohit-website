export enum HideOverflow {
    SHARE_POPUP,
    SCANNED_ITEM_POPUP,
    GOOGLE_DRIVE_PICKER,
    WEBSITE_MENU
}

export enum HideCursor {
    WEBSITE_COVER,
    GAMEPAD
}

export enum DisableUserSelect {
    FS_SCROLL_BAR,
    WEBSITE_COVER
}